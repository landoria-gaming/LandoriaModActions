name: Publish rolling snapshot
description: Replace the snapshot prerelease only from current main
inputs:
  mod-name:
    description: Mod assembly name
    required: true
  package-name:
    description: Thunderstore package name without Landoria prefix
    required: true
  token:
    description: GitHub token with contents write permission
    required: true
runs:
  using: composite
  steps:
    - shell: bash
      env:
        ACTION_DIRECTORY: ${{ github.action_path }}
        GH_TOKEN: ${{ inputs.token }}
        MOD_NAME: ${{ inputs.mod-name }}
        PACKAGE_NAME: ${{ inputs.package-name }}
        GH_REPO: ${{ github.repository }}
      run: bash "$(cygpath -u "$ACTION_DIRECTORY")/../../tools/publish-snapshot.sh"
